"""Shared constants, paths, and color maps for the ROI calculator."""

from pathlib import Path

# ─── Paths ───────────────────────────────────────────────────────────────────────

CLAUDE_DIR = Path.home() / ".claude"
PROJECTS_DIR = CLAUDE_DIR / "projects"
USAGE_DIR = CLAUDE_DIR / "usage-data"
ROI_FACETS_DIR = USAGE_DIR / "roi-facets"
ROI_CONFIG_FILE = USAGE_DIR / "roi-config.json"
ROI_REPORT_FILE = USAGE_DIR / "roi-report.html"

# ─── LLM Config ──────────────────────────────────────────────────────────────────

MAX_FACET_TOKENS = 2048
MAX_ANALYSIS_TOKENS = 4096
MAX_SUMMARY_TOKENS = 2048

# ─── Pipeline Config ─────────────────────────────────────────────────────────────

MAX_NEW_SESSIONS = 50
SUBSCRIPTION_COST_USD = 100.0
TRANSCRIPT_SUMMARIZE_THRESHOLD = 30_000
CHUNK_SIZE = 25_000
MAX_MANUAL_MULTIPLIER = 10

# Marker to detect sessions generated by this script (to avoid self-analysis)
INTERNAL_MARKER = "You are analyzing a Claude Code session to estimate its ROI"

# ─── Display Maps ────────────────────────────────────────────────────────────────

VERDICT_COLORS = {
    "strongly_positive": "#16a34a",
    "positive": "#65a30d",
    "neutral": "#737373",
    "negative": "#dc2626",
    "strongly_negative": "#7f1d1d",
}

EFFICIENCY_COLORS = {
    "highly_efficient": "#16a34a",
    "efficient": "#65a30d",
    "neutral": "#737373",
    "inefficient": "#f97316",
    "counterproductive": "#dc2626",
}

OUTCOME_COLORS = {
    "fully_achieved": "#16a34a",
    "mostly_achieved": "#65a30d",
    "partially_achieved": "#eab308",
    "not_achieved": "#dc2626",
    "unclear": "#737373",
}

CATEGORY_LABELS = {
    "debug_investigate": "Debug & Investigate",
    "implement_feature": "Implement Feature",
    "fix_bug": "Fix Bug",
    "write_script_tool": "Write Script/Tool",
    "refactor_code": "Refactor Code",
    "configure_system": "Configure System",
    "create_pr_commit": "Create PR/Commit",
    "analyze_data": "Analyze Data",
    "understand_codebase": "Understand Codebase",
    "write_tests": "Write Tests",
    "write_docs": "Write Docs",
    "deploy_infra": "Deploy/Infra",
    "warmup_minimal": "Warmup/Minimal",
}

# ─── Logging ─────────────────────────────────────────────────────────────────────

def log(step: str, detail: str = "") -> None:
    if detail:
        print(f"→ {step:<30} {detail}")
    else:
        print(f"→ {step}")
